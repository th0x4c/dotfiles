#name : foo { ... } ; setFoo { ... }
# --
- (${1:id})${2:foo}
{
  return $2;
}

- (void)set${2:$(upcase-initials text)}:($1)aValue
{
  if ($2 != aValue)
  {
    [$2 release];
    $2 = [aValue retain];
  }
}
$0
