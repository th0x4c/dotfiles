;; cc-mode

;; Indentation of a brace that opens a substatement block(if/while/for/do/else)
(setq c-offsets-alist '((substatement-open . 0)))

;; Prevent extraneous tabs
(setq-default indent-tabs-mode nil)

